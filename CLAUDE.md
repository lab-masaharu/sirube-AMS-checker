# CLAUDE.md — sirube-ams-checker 開発規約・設計思想

> このファイルは Claude Code が実装前に必ず読む規約である。
> 設計思想・確定事実・標準・現在地・進め方を記す。
> **設計はチャットのClaude（議論・判断）、実装はClaude Code（このファイルに従う）** の二段構え。
> system_code: `sys_ops_ams_checker` / リポジトリ: `sirube-ams-checker`

---

## 1. プロジェクトの目的と本質

AMS（自社開発の物件管理システム / agent-master.jp）に登録された各物件のステータスと、
その物件の元情報（媒体：ふれんず/SUUMO/athome/HOME'S/自社等）の実際のステータスを突き合わせ、
**「AMSの更新が必要な物件」を検知する**システム。

- **本質**: 現在、AMSのステータス（公開/商談中/成約済）は人間が媒体サイトを目視し手で更新している。
  その「更新が必要かどうかの判断」を、人手の目視から仕組みに変える。
- 比較するもの:
  - AMS側ステータス = 人間が前回手で入れた値（古い可能性がある＝過去）
  - 媒体側ステータス = 今その媒体に出ている実際の状態（現在の真実）
  - この2つがズレていたら「AMS更新が必要」としてアラート。
- 属人化していた目視確認を排し、判断を仕組みに落とすのが狙い（sirube思想の核心）。

---

## 2. 設計の基軸（揺らさない）

1. **AMSをデータベースとみなす（A案）**: RPA的に人の操作を写すのではなく、AMSを「データソース」として扱う。
   ブラウザ操作はデータを読み出す手段にすぎない。これにより将来の物件マスタ化・移行に地続きでつながる。
2. **媒体アダプタ方式（プラガブル構造）**: 媒体ごとにステータスの表示も語彙も違う。
   媒体ごとに独立したアダプタ（n8nノード）を作り、各アダプタが「その媒体の独自表現」を
   「全媒体共通の標準ステータス」に翻訳して返す。翻訳ロジックは各アダプタ内に閉じ込める。
   新媒体はアダプタを足すだけ。本体（突合エンジン）は標準ステータスだけ見る。
3. **84%先行・段階的100%化**: ふれんず462件＋主要ポータル（SUUMO/athome/HOMES）で約84%をまず完成。
   残り（other/レインズ/他社ページ）は後からアダプタ追加で100%化。
   **未対応媒体は `pending` として記録し可視化する**＝手動確認を見えないまま放置しない。
   これにより「1件でも手動確認が残ってマニュアルが太る＝属人化」を防ぐ。
4. **人間の確認・承認を必ず残す**: Phase 0は検知・通知のみ。AMSへの書き戻しはしない。
   書き戻しは将来フェーズで、人間が承認したものだけ。

---

## 3. アーキテクチャ全体図


- オーケストレーション層 = n8n（流れが見える・媒体追加が容易）。ただしn8n化は実装が動いてから。
- 重い実処理（Playwrightでのブラウザ操作）= コード側。n8nから呼ぶ。
- **開発順序**: ローカルNodeで動作確認 → 動いてからDocker/n8nで包む（移植性のため）。

---

## 4. データ構造

PostgreSQL（Docker、ホスト側ポート **5544**）。sirube標準の共通カラムに準拠。

### properties（物件マスタ＝将来の物件マスタの「種」。変わりにくい素性）

- id / tenant_id / object_id（AMS物件ID・業務キー・ユニーク） / media / media_url / freins_id
- ams_status（最後に観測したAMSステータス） / first_seen_at / last_seen_at
- status（active / gone＝巡回で消えた） / system_code / created_at / updated_at
- 2回目以降の巡回で、同一object_idは更新、消えた物件は status='gone' に。物件の出入りが時系列で追える。

### status_checks（差分ストア＝巡回のたび増える観測ログ・履歴）

- id / tenant_id / object_id / checked_at / ams_status / media / media_status
- judgment（match / mismatch / vanished / not_found / error / pending）
- review_status（pending / approved / rejected） / reviewed_by / reviewed_at
- note / run_id（1回の巡回をまとめるID） / system_code / created_at / updated_at

### v_latest_diffs（差分抽出ビュー）

- 各object_idの最新の突合結果のうち、judgment が mismatch/vanished/not_found/error のものだけを出す。
- Phase 0の目的「差分のある物件を抽出する」を1クエリで実現。

### 判断ファイル基盤（**将来の設計・今は実装しない**）

- 詳細は別途 docs に置く。概要のみ記載:
  - `decisions/<tenant_id>/<systemCode>/dec_<YYYYMMDD>_<連番>.md`（append-only・1判断1ファイル・不変）
  - Markdown + YAML front matter。覆ったら新ファイル＋supersedesで旧IDを指す。
  - proposedBy（AI・任意）と decidedBy（人間・必須）を分離。人間欄が空の判断は不正として弾く。
  - factSnapshot は最小4フィールド固定（statusAtDecision/verifiedAtDecision/tierBefore/tierAfter）。肥大させない。
  - Postgres側に trust_tier（human_verified/agent_generated_unverified/external_raw）等を将来追加。
  - accessLevel（誰に見せてよいか）と trust_tier（どれだけ信じてよいか）は直交。混ぜない。
  - **この基盤は差分検知が動いてから着手する。先回りして作らない。**

---

## 5. 確定した技術事実（実証済み・推測ではない）

### AMS（agent-master.jp）

- ログイン: 入力 `#ipt_user_email_1` / `#ipt_user_password`、ボタン `#btnLogin`。
  ログイン後 `https://agent-master.jp/` のトップ（AMSロゴ＋News）に遷移。リストはまだ出ていない。
- リスト表示の操作フロー（実証済み）:
  1. トップで「検索画面」リンク（`a:has-text("検索画面")`）をクリック → colorboxのiframe(`search.html`)が開く
  2. **iframe内**でステータスのチェックボックスを操作:
     `#obj_status1_chk`=公開 / `#obj_status2_chk`=商談中 / `#obj_status3_chk`=成約済 / `#obj_status4_chk`=売止め
  3. **iframe内**の「リストで表示」ボタン `a.btnExec`（img alt="検索:リストで表示"）をクリック
  4. 結果は**親ページ**に出る（iframeではない）。`#hidden_object_count` に総件数、`tr[data-href]` が物件行（30件/頁）
- ページング: 親ページの関数 `getObjectList(page_num)` を呼ぶ（`onclick="getObjectList('2')"` 形式）。
  `#page_num` hidden あり。最終ページは件数÷30の切り上げ。
- 各物件行 `<tr data-href="{object_id}">` 内の `<a href target="_blank">` が媒体リンク。
  詳細(object_detail)・CMA(/cma/)リンクは媒体ではないので除外。
- **ステータス別取得（重要・実証済み）**: 検索画面のステータスチェックは絞り込みフィルタ。
  「公開」だけチェック→公開736件のみ表示 / 「商談中」だけ→40件 / 両方→776件。
  → **ステータスごとに分けて巡回すれば、リストに出た事実がそのままAMSステータスになる**（列を読む必要なし）。

### ふれんず（b2b.f-takken.com）

- 詳細ページURL: `https://b2b.f-takken.com/properties/detail?id=<freins_id>`
- freins_id は12桁（例 000002488843）。AMSリスト行の媒体URLから `[?&]id=(...)` で抽出。
- ログインはID/PWのみ（2段階認証なし）。※ログイン構造・ステータス表示位置は**未調査**（次タスク）。

### 取得実績（2回実行・安定）

- 総件数 778件前後（日により増減）。媒体内訳: freins 462 / suumo 134 / athome 42 / homes 12 / other 72 / none 56。
- ユニークobject_id 778・重複0。全ページ取りこぼしなし。
- other の正体: 他社不動産ページ（livable/stepon/rehouse/e-blooming等）とレインズ(reins.jp)。Phase B回し。

### 突合の方向（重要）

- 主目的は「AMSで公開/商談中になっている物件が、媒体側で先に動いていないか（商談中/成約済になっていないか）」。
- 特に**公開736件こそ、媒体側で変化していないか確認する対象**（公開だからmatchで済む、ではない）。
- 対象は公開＋商談中の776件全部。ふれんず462件は全件、媒体側の実ステータスを取得して突合する。

---

## 6. 判定の標準語彙（勝手に増やさない）

### 媒体標準ステータス（各アダプタが翻訳して返す共通語）

- `open`（公開中） / `negotiating`（商談中） / `contracted`（成約済） / `vanished`（掲載消失） / `unknown`（判別不能）
- ふれんずは3段階フル判定可（公開/商談中/成約済）。
- SUUMO/athome/HOMES等のポータルは構造上「掲載あり=open / 消失=vanished」の2値が限界
  （商談中・成約済はポータルに情報が存在せず区別不能）。これは情報の不在であり、作り込みでは解決しない。

### judgment（突合結果の区分）

- `match`（一致＝更新不要） / `mismatch`（食い違い＝AMS更新必要） / `vanished`（媒体から消失＝要確認）
- `not_found`（リンク先が開けない/該当なし） / `error`（取得失敗） / `pending`（アダプタ未実装・未対応）

---

## 7. sirube標準の遵守事項

- **記録なき操作を禁止**: すべての判断・更新は記録を残す。人間欄が空の判断は不正。
- **方針決定は人間、AIは判断できる状態を作る役割**: proposedBy(AI) と decidedBy(人間) を分離。
- **ステータス標準値を勝手に増やさない**: 新コードが要るなら先に MASTER_DATA.md を改訂してから実装。
- **横断ID**: systemCode / entityType / entityId / status を最初から揃える。
- **シークレット管理**: 認証情報は `.env` のみ。コードにハードコード禁止。`.env` はGit除外、`.env.example` はテンプレ（空）。
- **構造化ログ（JSON）**: timestamp / level / systemCode / service / action / metadata。
- **Git管理**: ファイル名に `_v2` 等を付けない。版管理はGitで行う。監査ログ・判断は削除しない（append-only）。
- **AIに restricted/system_only 情報を渡さない**。
- **エージェント間は直接通信せず n8n/API を中継**。Webhookは署名検証＋冪等性＋即時200。

---

## 8. 現在地と次のタスク

### 完了

- AMS巡回スクリプト（crawl-ams.js）で778件取得成功（媒体仕分け・ふれんずID抽出込み）。
- PostgreSQL（Docker, port 5544）構築。properties / status_checks / v_latest_diffs 作成済み。
- load-to-db.js で778件を properties に投入済み（全 active）。

### 次のタスク（この順序）

1. **crawl-ams.js をステータス別巡回に改修**: 「公開のみ」「商談中のみ」の2パスで巡回し、
   各物件に amsStatus（公開/商談中）を付けて results.json に保存。load-to-db.js で ams_status に反映。
2. **ふれんずのログイン構造・ステータス表示位置を調査**（診断スクリプト）。
3. **freinsアダプタを作る**: ふれんず462件の詳細ページから実ステータスを取得し標準語に翻訳。
4. **突合エンジン**: ams_status と 媒体標準ステータス を比較し judgment を決定、status_checks に記録。
5. **差分抽出**: v_latest_diffs で mismatch/vanished を一覧化（＝Phase 0目的達成）。
6. その後: SUUMO等アダプタ追加 → n8n化 → Docker包括 → 判断ファイル基盤。

---

## 9. 進め方のルール

- **設計・方針・判断はチャットのClaudeで議論し、森さんが決定する。実装はClaude Codeがこのファイルに従って行う。**
- 段階的に確認しながら進める（一気に大量変更しない）。各ステップで森さんの確認を取る。
- **過剰エンジニアリングを戒める**: まだ来ていない負荷に備えた器を先に作らない。
  1フォーマット/1実装で始め、実測で重いと分かってから分離・最適化する。
  （森さん自身が自覚している傾向。Claude Codeはこれを増幅しやすいので特に注意）
- 取得対象の媒体サイトには礼儀ある間隔（待機）でアクセスする。
- 動くコードだけが完成ではない。判断・設計・なぜそうしたかをGitに資産として残す。
- このプロジェクトはレガシー（AMS）からAIエージェント運用への移行の実例であり、
  その移行プロセス自体を再現可能な方法論として資産化することが最上位の価値。

---

## 10. 本番システムへの絶対禁止事項

AMS（agent-master.jp）およびふれんず（b2b.f-takken.com）は本番稼働中の業務システムである。
これらへのアクセスは**読み取り（閲覧・検索・一覧取得・詳細閲覧）のみ**を許可する。

### 絶対に行わない操作

- データの登録・更新・削除
- 「登録」「保存」「削除」「複製」等のボタンのクリック
- 検索条件指定を除くフォーム入力
- ステータス変更（物件状態・公開状態・商談状態等）

### 許可される操作の定義

「**人間が閲覧目的で行う操作と同等のもの**」のみ許可する。

具体的には: ログイン・検索条件の指定・検索実行・一覧の閲覧・詳細の閲覧・ページめくり。

### 検索条件チェックボックスについての解釈

検索画面（search.html iframe内）でのステータスチェックボックス（`#obj_status1_chk`〜`#obj_status4_chk`）の
操作は「**検索条件の指定**」であり、物件状態の変更ではないため許可する。
ただし、**検索画面以外**でのボタン・チェックボックス操作は一切禁止する。

### 実装上のルール

- コード中のクリック操作は `safeClick()` 経由のみ許可し、ホワイトリスト（`ALLOWED_CLICK_SELECTORS`）に
  登録されたセレクタ以外はすべて `throw` して即停止する。
- ページめくりは `getObjectList()` 固定名のみ許可。`evaluate()` 経由で他の関数を呼ばない。
- **書き込み系の操作要素に触れる実装が必要になった場合、実装せず必ず停止して森さんに確認する。**

---

## 11. ふれんずアダプタ仕様

### 詳細ページ URL

`https://b2b.f-takken.com/properties/detail?id=<freins_id>`

### ログイン

auth.f-takken.com 経由のSSO。会員メール・パスワードは `.env` の `FREINS_EMAIL` / `FREINS_PASSWORD` から読む。

### 判定ロジック（body の class で分岐）

- **`body class` に `"error-page"` を含む、または本文に「該当物件情報がありませんでした」** → `vanished_suspected`（消失の疑い）。  
  1回の 404 は疑い。複数回連続で `vanished`（濃厚）。成約済・取り下げ・登録満了でふれんずはページごと 404 になる（実物確認済み）。
- **`body class` に `"detail"` を含む（物件ページ）** → `.info-label` が「取引状況」の直後の `.info-val` のテキストを取得。これを「取引状況原文」として必ず保持する。
- **auth.f-takken.com へリダイレクト** → セッション切れ。`judgment=error`（物件消失ではない）。
- **タイムアウト / 403 / 429 / ネットワークエラー** → `error`。429 やアクセス拒否は即座に全体停止。

### 取引状況原文 → 標準ステータス翻訳テーブル（既知表現）

| 取引状況原文 | 標準ステータス |
|---|---|
| 「公開中」 | `open` |
| 「商談中」「ただいま商談中です」 | `negotiating` |
| 「書面による購入申込あり」「書面による購入申込みあり」（申込み系） | `negotiating` |
| 上記いずれにも一致しない未知表現 | `unknown`（原文を必ず保持） |

### 突合と要注意フラグ

- AMS側ステータスが「公開」なのに、ふれんず取引状況が「公開中」以外（`open` 以外）→ `judgment=mismatch`、かつ「要注意・人間確認」フラグを立てる。`note` にふれんず取引状況の原文を丸ごと記録。
- 表現の揺れ（担当者が自由記述している可能性）に備え、未知表現でも「公開中ではない記載がある」こと自体を検知できる設計とする。完全な意味解釈は将来の AI 監視に委ね、まずは原文保持＋人間確認フラグを土台とする。

### 記録方針

`status_checks` に `judgment`・`note`（ふれんず取引状況原文）・`media_status`（翻訳後標準ステータス）を記録。`v_latest_diffs` に `mismatch` / `vanished` が出る。

### 本番アクセスの慎重策（ふれんずは規約グレー・他社本番サービス）

- 1件あたり 4〜6秒のランダム待機。
- 本巡回は一度に全件でなく上限を設け分割（上限は引数で指定。§13 参照）。
- 403 / 429 / 連続エラーで即座に全体停止。
- AMS と同じく、クリックはホワイトリストに限定。書き込み系（登録/保存/削除/申込ボタン等）は一切触らない。閲覧のみ。
- ホワイトリスト実装上の注意: `button[type="submit"]` はSSO ログイン送信ボタンとして使用するが、セレクタ文字列に `"submit"` が含まれるため **禁止語リストから `"submit"` は除外**する。代わりに `"申込"` `"apply"` `"inquiry"` 等をより具体的に禁止する。

---

## 12. 運用設計（Phase 0 大野城店）

### 動作リズム

- 深夜0時に起動し、処理を深夜のうちに終え、朝9時の出勤に結果が手元に届くようにする。
- 結果の届け方（メール/ファイル/画面）は別途決定。当面は差分リストを出力する。

### 巡回と分割

- ふれんず対象（media=freins, status=active）を100件ずつのバッチに分割して巡回する。
- 現状の対象は472件 = 100件×4 + 72件。
- 日中の手動確認では森さんが1バッチずつ起動して確認。無人運用ではバッチを自動連結する（無人化は後の段で実装）。

### 404の再確認ロジック（最重要）

- 1巡目で404（vanished_suspected）だった物件だけを「再確認キュー」に入れる。
- 明示的にステータスが読めた物件（match も、商談中等の mismatch も）は1巡目で確定し、再確認しない。
  理由: 404は「情報の不在」で揺れる（反映待ち・一時不調がありうる）。明示ステータスは「情報がある」ので1回で確実。
- 再確認キューの物件を、指定間隔を空けて「さらに3回」再取得する（1巡目を含めると計4回アクセス）。
- 再確認の途中で物件ページが復活（detail が読める）したら、一時的な反映待ちだったとみなし、その時点のステータスで確定してキューから外す。
- 3回の再確認すべてで404が続いた物件（計4回404）だけを vanished 確定とし、差分リストに載せる。
- 間隔は本番30分。テスト時は短く（2〜3分）してロジックの正しさだけ先に確認する。

### 無人運用の安全（後の段で実装）

- 無人で本番（AMS/ふれんず）にアクセスするため、異常時（403/429/連続エラー）は即座に全体停止。
- 翌朝「停止した」と分かる停止ログを残す。暴走して延々アクセスし続けることが絶対にないようにする。

### 動作パラメータは引数管理（env禁止）

- 再確認間隔・バッチ件数・再確認回数・オフセット等の動作パラメータは、すべて引数で渡す。
- これらを .env に置くことは禁止（理由は §13 を参照）。
- 将来、これらは設定画面から操作できるようにする。

### 将来の4店舗対応（今は実装しない）

- 大野城店のほか3店舗、計4店舗を回す前提。AMS・ふれんずとも店舗ごとに別アカウント。
- まず大野城店でPhase 0を完成させ、その後4店舗化する。
- そのため、店舗固有の値（認証情報・店舗名）はハードコードせず、店舗単位の設定として外出しできる素地を残す（媒体アダプタ方式と同じ発想で、店舗も差し替え可能な単位にする）。
- 完全な4店舗対応の作り込みはしない（来ていない負荷に器を先に作らない）。

---

## 13. セキュリティ設計原則（全システム共通・最重要）

### 秘密情報と動作設定の層分離

- .env には秘密情報（ID・パスワード・APIキー・シークレット）のみを置く。
- 動作パラメータ（間隔・件数・回数等の運用設定）を .env に混ぜない。引数、将来は設定画面で扱う。
- 理由: 運用設定は変更頻度が高い。それを .env に置くと、設定変更のたびに秘密情報のある場所を開くことになり、秘密情報へアクセスできる人が不必要に増える。
- 秘密情報へのアクセスは管理者のみに限定し、一般の設定操作（設定画面）とは経路を完全に切り離す。
- 設定をいじる人が秘密情報に触れられない構造そのものが防御になる。
- これは「セキュリティの脅威を受ける窓口を最小にする」設計（権限最小化・関心の分離）。
- 本原則は ams-checker だけでなく、シルベオフィスの全システムに適用する設計指標である。
  （sirube-office-standards のセキュリティポリシーに反映すべき項目。sirube-account-manager の存在理由と一致する。）

---

## 14. データガバナンス（基底ポリシー / 上書きポリシー）

### 枠組み

シルベオフィスのデータ管理・保持期限・セキュリティは、2層で管理する。

- **基底ポリシー**: 全システム共通の土台。将来 sirube-office-standards に整備する（現時点では未整備。システムが増え、共通項が実例から見えた段階で抽出・格上げする）。
- **上書きポリシー**: 各システム固有の方針。そのシステムの目的に応じて、基底ポリシーを必要な分だけ上書き・具体化する。各システムの CLAUDE.md に記載する。

本章は ams-checker の「上書きポリシー」である。

### ams-checker 上書きポリシー: データの分類と扱い

ams-checker のデータは、寿命（保持期間）の違いで3種類に分かれる。

**1. 現在の事実の台帳（properties テーブル）**

- AMSから取得した物件マスタ（物件番号・名称・ステータス・freins_id 等）。「今この瞬間AMSにどんな物件があるか」という現在の事実の台帳。
- 廃棄しない。毎日の巡回で最新に上書き更新する（UPSERT、消えた物件は gone 化）。
- 古い情報は上書きで自然に消えるため、「古い材料で突合しない」狙いはこれで満たされる。
- これは「廃棄」ではなく「最新化」の対象である。

**2. 突合用の一時材料（results.json、ふれんず取得の生データ等）**

- 突合のためだけに一時的に発生する材料（AMS巡回の生出力 results.json、ふれんずから取得した取引状況の原文の生取得など）。突合が終われば役目を終える。
- 突合完了後に廃棄する。保持しない。
- 理由: 突合の道具であり、突合後に残す意味がない。残すと「いつの材料か」が曖昧になり判断を誤らせる。
- 注: ふれんず取引状況の原文のうち、検知結果の根拠として status_checks の note に記録されたものは「検知結果」の一部として扱い、3.に従う（1ヶ月保持）。廃棄対象は、突合に使った生取得そのもの（独立した一時ファイル・一時データ）である。

**3. 検知結果（status_checks テーブル）**

- 「この物件はAMS公開だがふれんずで消失していた」等の突合の結論。判断の記録。
- 1ヶ月間保持する。1ヶ月を過ぎたものは廃棄する。
- 理由: 朝の対応の根拠であり、対応漏れ確認・検知精度の検証に使う。ただし無限には溜めず1ヶ月の有限の窓とする。append-only で判断の経緯（再確認の履歴等）も残す。

### 廃棄の作法（セキュリティ）

- 廃棄は「確実に消える形」で行う（DBから物理削除、ダンプ等の残骸を残さない）。
- 廃棄したこと自体は監査ログに残す（いつ・何を・何件廃棄したか）。データは消すが「廃棄した事実」は記録する（no-operation-without-record に沿う）。
- 本システムは突合データを溜め込まない設計であることを、外部（システムベンダー・将来のクライアント・媒体側への配慮）に文書で説明できる状態を保つ。

### 現状の保管場所（事実の記録）

- 現在、データは森さんのPC上の Docker PostgreSQL（sirube_ams、ポート5544）の1箇所のみに存在する。
- 将来の保管場所（VPS移行 / System Hub 集約等）は未定。Phase 0 完成後に検討する。

---

## 15. 最終ゴールと位置づけ（短期 / 中期 / 長期）

### 本システムの位置づけ

- ams-checker は単独の検知ツールではなく、より大きな構想の第一歩（足掛かり）である。
- 現在のAMS（agent-master.jp）は10年もののjQuery+colorbox+iframe+PHP製で、制作者が不在、最新化（機能追加・改修）が困難なオーファン（孤児）システムである。
- このAMSはいずれ使わない。最終的には複製し、バックエンドをAPI/AIを組み込んだ形で再構築する。
- 現在RPA（ブラウザ操作の自動化）で人手の確認作業を機械に置き換えているのは、その再構築への足掛かりである。

### 短期ゴール（Phase 0・現在実装中）

- AMSと媒体（ふれんず先行）のステータス突合を機械化し、人間が確認すべき箇所だけを残して朝9時に届ける。
- 当面の朝の成果物は「差分リスト（消失・要確認の一覧）」で十分とする。物確資料の形は中期で目指す。

### 中期ゴール（物確資料の自動生成）

- 現場で人間が手で使っている「物確資料」（物確202602.xlsx と同じ列構成・見た目）を自動生成する。
- AMS由来の列（物件番号・種目・名称・価格・現況・ステータス・業者名等）を自動で埋める。
- 媒体掲載状況の列（HP・SUUMO・at・ふれんず等）を媒体アダプタの突合結果で自動で埋める。
- 人間が電話確認して○を付ける列（確認・OC・NG等）は人間に残す。
- 見方（列順・見出し・○の付け方）は現行資料を踏襲する（「見方を揃える」ポリシー：現場が見慣れた形のまま使えること）。
- 媒体アダプタ（SUUMO/athome/HOME'S）を順次追加し、自動で埋まる列を増やす。
- ステータスだけでなく、住所等の属性の突合も将来の検知対象に含める（AMSと媒体で属性が食い違っていないかの確認）。

### 長期ゴール（真の最終形・AMS再構築）

- 現在のAMS（オーファン）を複製し、バックエンドをAPI/AI組み込みで再構築する。
- ams-checker はその起点＝ストラングラーフィグ・パターンの最初の蔓。古いAMSを外側から機械化して包み、機能を段階的に切り出し、最終的に新バックエンドへ移す。
- 現在の「読み取り専用（書き込み一切禁止）」設計は、安全ポリシーであると同時に移行戦略でもある。捨てる予定の古いAMSに深く作り込まず、新システムへ移りやすい形を保つ。古いAMSへの自動入力（書き込み）は作らない。
- この「古い属人システムを、止めずに、安全に、新しい知能を持つシステムへ移す」プロセス自体を、再現可能な方法論として資産化することが最上位の価値（他クライアントにも展開可能な商品となる）。

### この章が実装判断に与える指針

- 今のAMSは「いずれ捨てる対象」である。AMS依存のコードは深く作り込まず、薄く・差し替え可能に保つ。
- 新システムへの移行を妨げない形を優先する。

---

## 16. 今後のロードマップと方針決定

### Phase 0 完了状況（2026-06-16 時点）

- ふれんず472件（実466件）の全件巡回・突合・404再確認まで実証完了。
- 検知結果: match約401・mismatch 8・vanished 56前後（要確認物件を約14%に絞り込み）。
- 本番システムへの安全策（403/429即停止・連続エラー停止・4〜6秒待機・100件分割）が全件規模で機能することを確認。

### 今後のロードマップ

**1. Git運用**
- 動作中の版は main に残し業務継続利用。
- リファクタリングは `refactor/media-adapter-oop` ブランチで進める。
- 振る舞い（同じ物件→同じjudgment）は変えない。検証完了後にマージ。

**2. OOP整理（媒体アダプタ方式のコード化）**
- 基底 `MediaAdapter` ＋ `FreinsAdapter` ＋ 媒体非依存の共通部品（突合・記録・再確認）に分離。
- 媒体追加はアダプタ追加だけで済む構造にする。

**3. MCP化（ふれんず先行）**
- `FreinsAdapter` を「薄いMCPサーバー」（取得のみ・案A）として公開。
- 突合・記録・再確認はMCPの外（共通部品）に置く。
- 管理はMCP Inspectorで個別に行う。
- AMSのMCP化は後回し。

**4. n8n（指揮者として）**
- n8nは「全部やる場所」にしない。オーケストレーター（指揮・タイミング制御・結果の受け渡し・可視化・異常通知）に徹する。
- 重い実処理（クローラー・突合・再確認）はn8nの外の独立部品が担い、結果をn8nに渡す。
- 0時起動・朝9時出力・404再確認の連結を組む。

**5. 朝の成果物（Web管理画面）**
- Express・ローカルでの管理画面。要確認物件の一覧・差分内容・更新済マーク。
- Phase 0完成後に着手。VPS/System Hub集約は将来。

### Hermesエージェントの方針（記憶媒体としての活用）

- HermesをClaudeにMCP接続し、Claudeの外部記憶媒体の一つとして活用する（Nous Research製・MITライセンス・セルフホスト・ローカル保存）。
- **透明性の規律**: Hermesに保存する重要事項は森さんが確認できる形を保つ（透明性はCLAUDE.md水準を維持）。Claudeの外部記憶を森さんのブラックボックスにしない。
- **段取り**: ams-checkerのMCP化（ふれんず）を先に一周し、MCPサーバー構築とInspector管理を経験してから、その型でHermes接続に進む。
- **CLAUDE.mdの位置づけは不変**: CLAUDE.mdは本システムの「憲法・法律」であり、Hermesの内部記憶より上位。設計判断・禁止事項・ポリシーはCLAUDE.mdに明文化され、誰でも読め、Gitで履歴が残る。
